# Bazel writes everything under bazel-bin / bazel-out at the workspace
# root, not in this directory. macOS' Finder occasionally drops .DS_Store
# files anywhere; ignore them so they don't pollute commits.
.DS_Store
