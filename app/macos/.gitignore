# Generated by xcodegen — `project.yml` is the source of truth.
LoFi.xcodeproj/

# xcodebuild's derived-data tree (we point `-derivedDataPath` here).
build/
DerivedData/

# Xcode IDE state files, never reviewed.
*.xcuserstate
.DS_Store
